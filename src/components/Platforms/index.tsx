import * as React from "react";
import "./style.scss";

const Platforms: React.FC = () => {

    return (
        <div className="platforms">
           <span>...................</span>
           <span>...................</span>
           <span>...................</span>
        </div>
    );
}

export default Platforms;
